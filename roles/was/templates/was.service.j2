[Unit]
Description=WAS FastAPI Server
After=network.target

[Service]
User=ansible
Group=ansible
WorkingDirectory=/opt/was/app
EnvironmentFile=/etc/was.env
ExecStart=/opt/was/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
